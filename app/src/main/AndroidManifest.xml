<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cllasify.cllasify">

    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Cllasify">
        <activity android:name=".Presence"/>
        <activity android:name=".Social" />
        <activity android:name=".Fragment.Subscriptions" />
        <activity android:name=".EditProfile" />
        <activity android:name=".ProfileMine" />
        <activity android:name=".Account" />
        <activity android:name=".User_Question" />
        <activity android:name=".Phone_Verify" />
        <activity android:name=".Phone_Login" />
        <activity android:name=".Landing_Feed_Answer" />
        <activity android:name=".Landing_Feed" />
        <activity android:name=".Dashboard" />
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>